# Лабораторная работа №2: C++ BUILD / IF / LOOP, PYTHON

**Выполнил:** Даниил Н.
**Дата:** 25.06.2026

## 1. C++ EXPRESSION

Программа на C++ (expression.cpp) скомпилирована в папку build.
Вычисление: x^2-x^2+x*4-x*5+x+x в отдельной функции compute().
Цикл в main вызывает compute() n раз.
Замер времени через std::chrono.
Проверка ввода: если не число — выход.

Результат (1234 итерации):
- Общее время: 3 мкс
- Среднее на итерацию: 0.0024 мкс

## 2. PYTHON EXPRESSION

Программа на Python 3 (expression.py).
Та же логика, замер через time.time().

Результат (1234 итерации):
- Общее время: 230 мкс
- Среднее на итерацию: 0.1864 мкс

## 3. Сравнение быстродействия

C++ оказался примерно в 77 раз быстрее Python на одинаковом вычислении.

## 4. GIT

- Изменения зафиксированы в ветке dev
- Скриптами promote_dev_to_stg.sh и promote_stg_to_prd.sh накачены на stg и prd
- Созданы скрипты возврата rollback_dev.sh, rollback_stg.sh, rollback_prd.sh
- Папка build добавлена в .gitignore
